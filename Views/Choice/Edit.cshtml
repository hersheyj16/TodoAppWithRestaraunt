@model DotNetCoreSqlDb.Models.Choice

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>Choice</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ID" />
            <div class="form-group">
                <label asp-for="Suggester" class="control-label"></label>
                <input asp-for="Suggester" class="form-control" />
                <span asp-validation-for="Suggester" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="RestaurantID" class="control-label"></label>
                <select asp-for="RestaurantID" class="form-control" asp-items="ViewBag.RestaurantID"></select>
                <span asp-validation-for="RestaurantID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="EventID" class="control-label"></label>
                <select asp-for="EventID" class="form-control" asp-items="ViewBag.EventID"></select>
                <span asp-validation-for="EventID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
